# Test Fixtures

This directory will hold reusable fixture data for early product and platform validation.

Planned fixture groups:

- sparse-data Today and Recap fixtures
- second-user and shared-workspace fixtures
- photo fixtures with mixed metadata quality
- document and file fixtures for ingestion work

Fixture sets will be added in `S0-023` through `S0-025`.

# Mobile

This directory will host the EkamCore mobile client.

Planned direction:

- platform: `React Native`
- role: primary mobile gateway for Today, Recap, query, and card navigation
- first Sprint 0 expectation: import generated shared types once the contract workflow exists

The package manifest is intentionally minimal until the mobile shell is created.

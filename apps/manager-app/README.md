# Manager App

This directory will host the macOS manager app.

Planned Sprint 0 direction:

- platform: `Tauri`
- UI: `React + TypeScript`
- first milestone: `S0-007` project skeleton

The package manifest exists now so the workspace structure is stable before the app framework is bootstrapped.

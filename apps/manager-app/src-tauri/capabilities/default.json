{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the manager app shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

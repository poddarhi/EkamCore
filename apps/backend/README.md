# Backend

This directory is reserved for the EkamCore backend service.

Planned Sprint 0 direction:

- framework: `FastAPI`
- language: Python
- first milestone: `S0-015` backend skeleton with health, version, and stub product endpoints

The Python packaging/tooling choice will be added with the backend bootstrap task rather than guessed early in `S0-001`.
